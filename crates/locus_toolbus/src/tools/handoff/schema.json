{
  "name": "handoff",
  "description": "Hand off work to a new process that runs in the background. Returns immediately with a handoff_id; the command continues running without blocking the agent.",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "Shell command to run in the background"
      },
      "working_dir": {
        "type": "string",
        "description": "Working directory for the command (optional; defaults to repo root)"
      }
    },
    "required": ["command"]
  }
}
